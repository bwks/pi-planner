{
  "name": "pi-planner",
  "version": "0.1.5",
  "description": "Planner mode extension for pi",
  "private": false,
  "author": "bradmin",
  "license": "MIT",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "tsx": "^4.20.6"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
